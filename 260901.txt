행렬을 갖고 있는게 넘파이 밖에 없어서 넘사임ㅋ
=> 그래서 모든 곳에 무조건 쓰일 수 밖에 없음

-Numpy 슬라이싱
행을 출력함
smat(slicing_mat) = mat[0, :]
[0, :] => 0행 :(실행)

smat(slicing_mat) = mat[:, 0]
[:, 0] = > 1열 :(실행)

[[1,2,1],[0,1,2]]
=>1행 0열
=>2행 1열
=>1행 2열

-Numpy 연산
np.add() +
np.substract() -
np.multiply() *
np.divide() /

divide()를 쓰면 정수가 바로 실수로 바뀌는데?
=> 자료형 변환을 할 필요가 없다.
