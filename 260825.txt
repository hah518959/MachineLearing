--교재--
*위키독스*
딥러닝을 이용한 자연어 처리 입문

!ls -la
!ls: 파일 보여달라

-la: 모든 파일을, a: 숨겨진 것까지 전부

!pip list: 파일을 리스트화해서 보여달라
!pip list | grep ~~~: ~~~이 이름에 포함된 것들 보여달라

파이토치, 텐서플로: 데이터를 분석하는 장치

판다스와 넘파이: 안에서 데이터를 저장하는 라이브러리

import: 불러오기

pandas.__version__: 판다스의 버전

판다스의 데이터 구조는 2개로
1. Series: 
2. DataFrame: 

파이썬은 자료형을 자동으로 파악해서 앞에 자료형(int, char, string..)을 안 써도 구동한다.
**COLAB Series, DataFrame 같은 문법들 앞 대문자 구분함.**

-Series <= 메뉴판처럼 만들고싶다
-------------------------------------------------------------------------------------
sr = pd.Series([17000, 18000, 19000, 20000],
               index=["피자", "치킨", "콜라", "맥주"])
print('시리즈 출력:')
print('-'*15)
print(sr)
-------------------------------------------------------------------------------------
=> 여기서 value에 index값을 해당하게 하는게 Series.
[]<-대괄호가 1개임(1차원)
--> 판다스 시리즈를 쓰는 이유: 힘들게 리스트에서 그 리스트에 붙여주는 함수를 만들 필요없이 판다스를 불러와 바로 만들수 있기 때문이다.
-DataFrame <= 액셀처럼 만들고싶다.
-------------------------------------------------------------------------------------
values = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
index = ['one', 'two', 'three']
columns = ['A', 'B', 'C']

df = pd.DataFrame(values, index=index, columns=columns)

print('데이터프레임 출력 :')
print('-'*18)
print(df)
-------------------------------------------------------------------------------------
=>시리즈보다 컬럼이라는 행렬이 하나가 더 생김.
[[]]<-대괄호가 2개임(2차원)










